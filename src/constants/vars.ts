export const Easing = Object.freeze({
  EASE_OUT_QUART: 'var(--easeOutQuart)',
  EASE_IN_OUT_QUART: 'var(--easeInOutQuart)',
});

export const TransitionDuration = Object.freeze({
  HOVER: 'var(--hover-transition-duration)',
  ACTIVE: 'var(--active-transition-duration)',
});

export const ContainerSpacing = Object.freeze({
  PADDING_X: 'var(--container-padding-x)',
  GRID_GAP: 'var(--grid-gap)',
  GRID_COLUMNS: 'var(--grid-columns)',
});

export const Colors = Object.freeze({
  BLACK: 'var(--color-black)',
  WHITE: 'var(--color-white)',

  SUCCESS: 'var(--color-success)',
  ERROR: 'var(--color-error)',
  WARNING: 'var(--color-warning)',
  INFO: 'var(--color-info)',
});
